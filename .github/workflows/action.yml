name: Python Deployment Action

on:
  push:
    branches:
      - "master"
  pull_request:
    branches:
      - "master"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout actions
      - uses: actions/checkout@v4

      - name: Run python deployment script
        env:
          HOST: ${{secrets.HOST}}
        run: python server_conn_test.py
